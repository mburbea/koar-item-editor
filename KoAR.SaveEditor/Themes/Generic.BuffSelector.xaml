<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="clr-namespace:KoAR.Core;assembly=KoAR.Core"
    xmlns:constructs="clr-namespace:KoAR.SaveEditor.Constructs"
    xmlns:views="clr-namespace:KoAR.SaveEditor.Views">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/PresentationFramework.Aero,Version=4.0.0.0,PublicKeyToken=31bf3856ad364e35;component/Themes/Aero.NormalColor.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <constructs:EqualityConverter x:Key="EqualityConverter" />
    <constructs:ComboPanelMaxWidthConverter x:Key="MaxWidthConverter" />
    <DataTemplate x:Key="BuffToolTipTemplate" DataType="{x:Type core:Buff}">
        <views:BuffControl Buff="{Binding}" IsExpanded="True" />
    </DataTemplate>
    <Style TargetType="{x:Type views:BuffSelector}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type views:BuffSelector}">
                    <ComboBox ItemsSource="{Binding Buffs.Values, RelativeSource={RelativeSource TemplatedParent}}"
                        SelectedItem="{Binding SelectedBuff, RelativeSource={RelativeSource TemplatedParent}}"
                        HorizontalContentAlignment="Stretch" FocusVisualStyle="{x:Null}" TextSearch.TextPath="ShortDisplayText"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        x:Name="PART_ComboBox">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel IsItemsHost="True" MaxWidth="{Binding ActualWidth, ElementName=PART_ComboBox, Converter={StaticResource MaxWidthConverter}, ConverterParameter=400}" />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                        <ComboBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type core:Buff}">
                                <DockPanel Margin="0,1" x:Name="Panel">
                                    <views:RarityIcon DockPanel.Dock="Left" VerticalAlignment="Center" x:Name="RarityIcon" />
                                    <views:BuffIcon DockPanel.Dock="Left" VerticalAlignment="Center" Margin="2,0"  x:Name="BuffIcon" />
                                    <TextBlock TextTrimming="CharacterEllipsis" VerticalAlignment="Center" x:Name="DisplayText" />
                                </DockPanel>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding Converter={StaticResource EqualityConverter}}" Value="False">
                                        <Setter TargetName="Panel" Property="ToolTip">
                                            <Setter.Value>
                                                <ToolTip Content="{Binding}" ContentTemplate="{StaticResource BuffToolTipTemplate}" />
                                            </Setter.Value>
                                        </Setter>
                                        <Setter TargetName="RarityIcon" Property="Rarity" Value="{Binding Rarity}" />
                                        <Setter TargetName="BuffIcon" Property="Buff" Value="{Binding}" />
                                        <Setter TargetName="DisplayText" Property="Text" Value="{Binding ShortDisplayText}" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            </Style>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>