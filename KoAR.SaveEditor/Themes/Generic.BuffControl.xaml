<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:KoAR.SaveEditor.Views">
    <Style TargetType="{x:Type views:BuffControl}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type views:BuffControl}">
                    <DockPanel>
                        <views:BuffIcon DockPanel.Dock="Left" Buff="{TemplateBinding Buff}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Buff.ShortDisplayText, RelativeSource={RelativeSource TemplatedParent}}" Margin="2,0,0,0" 
                            VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>